# Skill Registry — SwiftShip

Auto-generated by sdd-init. Updated: 2026-04-26

## User Skills (global)

| Skill | Location | Trigger |
|-------|----------|---------|
| judgment-day | ~/.config/opencode/skills/judgment-day | "judgment day", "judgment-day", "review adversarial", "dual review", "doble review", "juzgar", "que lo juzguen" |
| sdd-apply | ~/.config/opencode/skills/sdd-apply | When the orchestrator launches you to implement one or more tasks from a change. |
| sdd-archive | ~/.config/opencode/skills/sdd-archive | When the orchestrator launches you to archive a change after implementation and verification. |
| sdd-design | ~/.config/opencode/skills/sdd-design | When the orchestrator launches you to write or update the technical design for a change. |
| sdd-explore | ~/.config/opencode/skills/sdd-explore | When the orchestrator launches you to think through a feature, investigate the codebase, or clarify requirements. |
| sdd-propose | ~/.config/opencode/skills/sdd-propose | When the orchestrator launches you to create or update a proposal for a change. |
| sdd-spec | ~/.config/opencode/skills/sdd-spec | When the orchestrator launches you to write or update specs for a change. |
| sdd-tasks | ~/.config/opencode/skills/sdd-tasks | When the orchestrator launches you to create or update the task breakdown for a change. |
| sdd-verify | ~/.config/opencode/skills/sdd-verify | When the orchestrator launches you to verify a completed (or partially completed) change. |

## Project Conventions

No AGENTS.md, CLAUDE.md, or similar convention files found in project root.

## Compact Rules

### NestJS / TypeScript (backend)
- Use NestJS module pattern: each feature gets its own module, controller, service, DTOs
- TypeORM entities in `src/entities/`, feature-specific logic in `src/modules/<feature>/`
- DTOs use `class-validator` decorators for validation
- Config via `@nestjs/config` with `.env` files
- Use `@nestjs/testing` for unit tests, `supertest` for e2e
- Jest config in `package.json`, tests match `*.spec.ts`
- TypeORM migrations via `pnpm migration:generate/run/revert`
- Data source config at `src/config/data-source.ts`

### Docker Compose
- Services: `api` (NestJS), `db` (PostgreSQL 16-alpine)
- Volumes for persistent data
- Health checks on database
- Environment variables via `.env`
